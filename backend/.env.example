PORT=5000
NODE_ENV=development

# JSON Web Token Secret
JWT_SECRET=sgpgi_nursing_prep_secret_key

# PostgreSQL Connection String (Optional, will fall back to local JSON database if empty)
# DATABASE_URL=postgres://username:password@host:5432/dbname
# DATABASE_SSL=true
